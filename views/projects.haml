%ul
  - @projects.each do |project|
    %li
      %a{:href => "/#{project.user}/#{project.name}"}
        %span{:class => project.state} &bull;
        :erb
          <span class = "lighter"><%= project.user %>/</span><%= project.name %>
